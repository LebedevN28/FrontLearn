import React from 'react';
import styles from './FrontLearnTitle.module.css'; 

const FrontLearnTitle: React.FC = () => (
  <h1 className={styles.title}>FrontLearn</h1>
);

export default FrontLearnTitle;